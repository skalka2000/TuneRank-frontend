import SettingsPanel from "../components/SettingsPanel";

function Settings() {
  return (
    <div style={{ padding: "2rem" }}>
      <h2>⚙️ Settings</h2>
      <SettingsPanel />
    </div>
  );
}

export default Settings;
